# 🚀 Space Fleet Management API

> Простой REST API для управления космическими кораблями и миссиями. Использует Flask, Marshmallow для валидации и внутреннюю ин-память хранилище данных.

---

## 📌 Описание проекта

Этот проект реализует API для управления космическим флотом:  
- Добавление, просмотр, обновление и удаление **космических кораблей**  
- Создание и управление **космическими миссиями**  
- Привязка кораблей к миссиям  

Все данные хранятся **в памяти** (в словарях и списках), что делает проект идеальным для обучения, прототипирования или тестирования.  
Для валидации входящих данных используется **Marshmallow**.

---

## 🏗️ Архитектура



### 🔧 Модули

| Файл | Описание |
|------|----------|
| `space_craft.py` | Определяет классы: `Spaceship`, `Mission`, `CrewMember` и их схемы валидации (`SpaceshipSchema`, `MissionsSchema`) |
| `logic.py` | Содержит функции для работы с данными: `add_ship()`, `update_ship_status()`, `add_mission()` и т.д. |
| `app.py` | Flask-приложение с REST-эндпоинтами, вызывающими логику из `logic.py` |

> 💡 **Важно**: Всё управление происходит по **имени** (`name`), а не по `id` — для упрощения отладки и наглядности. В продакшене лучше использовать `id`.

---

## 🌐 API Эндпоинты

### 🚀 Космические корабли

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `GET` | `/api/v1/shiplist` | Получить список всех кораблей |
| `POST` | `/api/v1/addship` | Добавить новый корабль |
| `GET` | `/api/v1/shipstatus/<name>/<newstatus>` | Обновить статус корабля |
| `DELETE` | `/api/v1/deleteship/<name>` | Удалить корабль |

#### ✅ Пример запроса: Добавить корабль

```http
POST /api/v1/addship
Content-Type: application/json

{
  "spaceship_id": 3,
  "name": "Star Destroyer",
  "type": "Imperial-class",
  "status": "available"
}

### Пример запроса - добавить миссию
POST /api/v1/addmission
Content-Type: application/json

{
  "mission_id": 1,
  "name": "Rescue Alderaan",
  "goal": "Save civilians from Imperial attack",
  "status": "planned"
}


### Пример запроса - привязать корабль к миссии
GET /api/v1/missionaddship/Rescue%20Alderaan/Star%20Destroyer

Устанавливаем зависимости:
pip install -r requirements.txt
